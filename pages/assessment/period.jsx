import React from 'react'
import { connect } from 'react-redux'


const period = () => {
  return (
    <div>
      
    </div>
  )
}

const mapState = (state) => {
  return {
    
  }
}

export default connect(mapState,{})(period);